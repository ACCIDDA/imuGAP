# Positive-integer count arguments native to a backend's sampler

Positive-integer count arguments native to a backend's sampler

## Usage

``` r
backend_int_args(backend)
```

## Arguments

- backend:

  one of `"rstan"` or `"cmdstanr"`.

## Value

a character vector of argument names that must be positive integers.
