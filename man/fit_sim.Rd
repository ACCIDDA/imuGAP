% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imuGAP-package.R
\docType{data}
\name{fit_sim}
\alias{fit_sim}
\title{Example Stan Fit}
\format{
A \code{stanfit} object as returned by \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.
}
\usage{
fit_sim
}
\description{
A reference \code{stanfit} object produced by running \code{\link[=imuGAP]{imuGAP()}} on the bundled
\code{locations_sim}, \code{populations_sim}, and \code{observations_sim} datasets. Intended
as a lightweight fixture for examples, tests, and downstream tooling that
needs a real fit without paying the cost of recompiling or re-running the
Stan model.
}
\details{
Generated with the same minimal sampler settings as the smoke test:
\itemize{
\item \code{iter = 100}
\item \code{chains = 1}
\item \code{seed = 1L}
\item \code{refresh = 0}
}

These settings are not enough for convergence; \code{fit_sim} is a wiring
fixture, not a scientifically meaningful posterior. The generating script
lives at \code{data-raw/fit_sim.R} and can be re-run from the package root with
\verb{Rscript data-raw/fit_sim.R}.

Note that \code{stanfit} objects bundle references to the compiled Stan model
and can be sensitive to major version changes in \code{rstan} and
\code{StanHeaders}. If a future install fails to load \code{fit_sim}, regenerate it
via \code{data-raw/fit_sim.R}.
}
\keyword{datasets}
