% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonicalize.R
\name{canonicalize}
\alias{canonicalize}
\alias{canonicalize_locations}
\alias{canonicalize_observations}
\alias{canonicalize_populations}
\title{Canonicalize imuGAP Data Objects}
\usage{
canonicalize_locations(locations)

canonicalize_observations(observations, drop_extra = TRUE)

canonicalize_populations(
  populations,
  observations,
  locations,
  max_cohort,
  max_age,
  max_dose = 2L
)
}
\arguments{
\item{locations}{a \verb{[data.frame()]}, with columns \code{loc_id} and \code{parent_id},
of the same type. See Details for restrictions.}

\item{observations}{a \verb{[data.frame()]}, the observed data, with at least
three columns:
\itemize{
\item an \code{obs_id} column; any type, as long as unique, non-NA
\item a \code{positive} column; non-negative integers, the observed number of
vaccinated individuals
\item a \code{sample_n} column; positive integers, the number of individuals
sampled, must be greater than or equal to "positive"
\item optionally, a \code{censored} column; numeric, NA (uncensored) or 1
(right-censored); if not present, will be assumed NA
}}

\item{drop_extra}{a logical scalar; drop extraneous columns? (default: yes)}

\item{populations}{a \verb{[data.frame()]}, the observation meta data, with
columns
\itemize{
\item \code{obs_id}, any type; the observation the row concerns (i.e. id shared with
an observations data object)
\item \code{loc_id}, any type; the location the row concerns (i.e. id shared with a
locations data object)
\item \code{dose}, a non-zero, positive integer (1, 2, ...); what dose row concerns
\item \code{cohort}, a positive integer; the cohort at the location row concerns
\item \code{age}, a positive integer; the age of that cohort row concerns
\item \code{weight}, a numeric, (0, 1); the relative contribution of this row to an
observation. Optional if each population row has a unique \code{obs_id}.
}}

\item{max_cohort}{if present, what is the maximum cohort that should be
present?}

\item{max_age}{if present, what is the maximum age that should be present?}

\item{max_dose}{maximum dose number to allow (default: 2L)}
}
\value{
\code{canonicalize_locations} returns a \code{data.table}, with:
\itemize{
\item \code{loc_id}, \code{parent_id} columns as originally supplied, possibly reordered
\item \code{loc_c_id}, \code{loc_cp_id} columns, canonicalized id/parent_id columns,
representing the order that will be used in the sampler
\item \code{layer} column, an integer from 1 (root), 2 (root children),
3 (grandchildren), &c
\item \code{layer_bound} column, an integer starting from 1 by layer. This provides
index slice information used in the stan model.
}

\code{canonicalize_observations} returns a canonical observation object, a \verb{[data.table()]} with:
\itemize{
\item an \code{obs_c_id} column, an integer sequence from 1; the order observations
will be passed to estimation
\item the original \code{obs_id} column, possibly reordered
\item \code{positive} and \code{sample_n} columns, possibly reordered
\item a "censored" column; all NA, if not present in original \code{observations}
argument
}

\code{canonicalize_populations} returns a canonical populations object, mirroring the input \code{populations},
with the following updates:
\itemize{
\item \code{obs_c_id}, the observation id the row concerns, canonicalized to match
the canonical observation ids
\item \code{loc_c_id}, the location id the row concerns, canonicalized to match
\item reordered to \code{obs_c_id} order
}
}
\description{
These functions validate, clean, and convert raw user-supplied data structures
(locations, observations, and populations) into the canonical forms required
by the \verb{[sampling()]} sampler and the underlying Stan models.
}
\details{
The \code{imuGAP} hierarchical modeling framework requires data structures to adhere to
specific relational and format constraints. The three canonicalize functions
process and validate these inputs:
\itemize{
\item \code{canonicalize_locations()} validates the geographic hierarchy structure (checks for unique root, no cycles, no duplicates) and assigns layers.
\item \code{canonicalize_observations()} validates observation counts (e.g. positive cases <= sample_n) and prepares censoring indicators.
\item \code{canonicalize_populations()} validates cohort, age, dose, and weight metadata and maps them to canonical observation and location IDs.
}

Each function returns a \code{data.table} with canonicalized sequential IDs (such as
\code{loc_c_id} or \code{obs_c_id}) matching the indexing expected by the sampler.
}
\examples{
# --- canonicalize_locations ---
data("locations_sim")
locations_sim
canonicalize_locations(locations_sim)
# can also be provided in non-canonical order, and with an implicit root
weird_locations <- subset(locations_sim, !is.na(parent_id))[
  sample(nrow(locations_sim) - 1L)
]
canonicalize_locations(weird_locations)
# --- canonicalize_observations ---
data("observations_sim")
observations_sim
canonicalize_observations(observations_sim)
# --- canonicalize_populations ---
data("populations_sim"); data("locations_sim"); data("observations_sim")
populations_sim
canonicalize_populations(populations_sim, observations_sim, locations_sim)
}
